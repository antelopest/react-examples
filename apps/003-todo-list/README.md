# To-Do List App

## Describe

Application for managing the to-do list. Create, delete, edit notes.

## React

* useState.
* React Forms (Controlled inputs, checkbox (completed, pending)).
* TSX (map, filter).
* Store (LocalStore, Zustand).
* useEffect (LocalStore, Zustand).
* Filters (all, done, active).
